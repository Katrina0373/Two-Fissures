Программа содержит класс Fissures, в котором при создании можно указывать значения l1, d1, l2, d2, N, k, p. Все методы и переменные класса специально сделаны публичными для отладки. Класс содержит методы 
fill_Ac(mat* A11, mat* A21, mat* A12);
	void fill_Areg(mat* A11, mat* A21, mat* A12, mat* A22);
	void fill_F